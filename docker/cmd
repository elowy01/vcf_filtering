docker build - -t classifier_filtering --build-arg USER_ID=$(id -u) --build-arg GROUP_ID=$(id -g) < classifier_filtering.Dockerfile

docker run --rm -it --name=classifier_filtering classifier_filtering
